options(
  warn = 1, 
  warnPartialMatchArgs = TRUE, 
  warnPartialMatchAttr = TRUE, 
  warnPartialMatchDollar = TRUE, 
  showWarnCalls = TRUE, 
  showErrorCalls = TRUE
)

library(checkpoint)
library(pacman)
